# Copy to deploy/staging.env (gitignored) — one-time WSL config:
#   cp deploy/staging.env.example deploy/staging.env
#
# Used by: ./deploy/scripts/staging-smoke.sh, aws-deploy.sh, functional-test-staging

# AWS ECS staging (primary)
STAGING_BASE_URL=https://api.staging.zettabridge.net
STAGING_ADMIN_EMAIL=admin@zettabridge.net
STAGING_ADMIN_PASSWORD=
