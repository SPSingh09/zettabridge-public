# Rendered when TELEGRAM_BOT_TOKEN is unset (Grafana-only / Telegram blocked).
# Alerts are accepted but not delivered until Telegram is configured.

global:
  resolve_timeout: 5m

route:
  receiver: "null"
  group_by: ["alertname", "environment", "severity"]

receivers:
  - name: "null"
