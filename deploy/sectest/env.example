# Staging pen-test env (copy to env.local — never commit)
SECTEST_BASE_URL=http://52.66.136.127

# When staging sets METRICS_TOKEN in runtime.env, set the same value here:
# SECTEST_METRICS_TOKEN=

# Optional flood burst size (default 25)
# SECTEST_FLOOD_BURST=25
