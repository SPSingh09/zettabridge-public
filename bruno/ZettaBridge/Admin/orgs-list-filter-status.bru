meta {
  name: GET List Orgs (filter status)
  type: http
  seq: 17
}

get {
  url: {{baseUrl}}/v1/admin/orgs?page=1&limit=50&status=active
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("Status 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
