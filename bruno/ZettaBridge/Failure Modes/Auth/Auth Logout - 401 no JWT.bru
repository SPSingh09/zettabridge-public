meta {
  name: Auth — 401 logout without JWT
  type: http
  seq: 42
}

post {
  url: {{baseUrl}}/v1/auth/logout
  body: none
  auth: none
}

tests {
  test("Missing token rejected", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
