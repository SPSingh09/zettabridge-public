meta {
  name: Signal — 403 suspended org
  type: http
  seq: 47
}

post {
  url: {{baseUrl}}/v1/webhook/{{orgWebhookToken}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "action": "BUY",
    "symbol": "EURUSD",
    "lot": 0.01
  }
}

docs {
  Prerequisite: platform admin suspends the org (`PATCH /v1/admin/orgs/:id` status=suspended).
  Expected: 403 organization suspended for org-owned webhooks.
}

tests {
  test("Suspended org rejected", function() {
    expect(res.getStatus()).to.equal(403);
  });
}
