meta {
  name: Credentials
  seq: 4
}

docs {
  Failure modes for /v1/credentials/* endpoints.
  Covers: empty raw_creds, invalid format, missing exchange for Indian brokers,
  verify format errors (hint field), missing SEBI algo_id on live credentials.
  Also: symbol-broker mismatch (forex symbol on Indian credential webhook).
}
