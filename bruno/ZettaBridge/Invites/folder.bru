meta {
  name: Invites
  seq: 7
}

docs {
  Public invite flow (E2):
  - GET  /v1/invites/:token
  - POST /v1/invites/accept

  Create invite first: Org > POST Create Invite → saves {{inviteToken}}.
}
